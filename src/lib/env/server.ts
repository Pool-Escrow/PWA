// Server env commented out until we need it
// export const env = createEnv({
//     server: {
//         TEST: z.string().check(z.minLength(1)),
//     },
//     experimental__runtimeEnv: process.env,
// })
